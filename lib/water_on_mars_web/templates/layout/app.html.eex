<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "WaterOnMars · Phoenix Framework" %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body style="background-image: url(<%= Routes.static_path(@conn, "/images/mars.jpg") %>); background-size: 100% auto;">
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <section class="phx-hero">
        <form action="<%= Routes.page_path(WaterOnMarsWeb.Endpoint, :heatmap) %>" method="post">
        <input id="sensor_data" name="sensor_data" type="text" placeholder="Enter sensor data">
        <button type="submit">Generate heatmap</button>
        </form>
      </section>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
